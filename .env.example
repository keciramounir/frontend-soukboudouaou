# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================

# API URL Configuration
# If your backend is on a different domain, set this to the full backend URL
# REQUIRED for Vercel separate deployments as the frontend and backend share the same origin
# Example: VITE_API_URL=https://backend-soukboudouaou.vercel.app
VITE_API_URL=https://backend-soukboudouaou.vercel.app

# Public Base Path
# If deploying under a subfolder, set this (e.g., /subfolder/)
# Leave empty for root deployment
VITE_PUBLIC_BASE=/

# Mock Data (Development Only)
# Set to 1 to enable mock data, 0 to disable
VITE_USE_MOCK=0
VITE_USE_MOCK_LISTINGS=0
VITE_USE_MOCK_USERS=0
